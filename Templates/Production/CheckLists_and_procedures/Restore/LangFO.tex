\def \txtOverskrift		{Checklisti til manuel restore roynd}
\def \txtKunde			{Kundi}
\def \txtDato			{Dagfesting}
\def \secScenario		{Vel eitt royndar-scenario}
\def \secWork			{Royndir - í raðfylgju}
\def \txtWorkA			{vSphere: VM'in nevnd {\TestMachineName} er staðfest at vera uppi og online.}
\def \txtWorkB			{Veeam: Tak nýtt backup av {\TestMachineName}.}
\def \txtWorkC			{vSphere: Steðga {\TestMachineName} aftaná liðugt backup og sløkk við Shutdown.}
\def \txtWorkD			{vSphere: Sletta {\TestMachineName}.}
\def \txtWorkE			{Veeam: Restore sambært valdum scenario (\textbf{A} ella \textbf{B})}
\def \txtWorkF			{\textbf{A} - {\TestMachineName} verður restore'að frá seinasta backup til somu lokatión og startað.}
\def \txtWorkG			{\textbf{B} - {\TestMachineName} verður startað beinleiðis frá backup, \\ síðan migrerað til produktión.}
\def \txtWorkH			{vSphere: Staðfest at {\TestMachineName} er uppi og online.}
\def \txtWorkI			{Veeam: Staðfest at backup job er korrekt, at rætt UID er sett fyri restore'aða maskinu.}
\def \secKonklution		{Niðurstøða}
\def \txtSuccess		{Er royndin roknað sum væleydnað?}
\def \txtJa				{Ja}
\def \txtNej			{Nei}
\def \txtNejTicket		{Uppgávulýsing skal skrivast.}
\def \txtStikord		{Stikkorð}
\def \txtDoneBy			{Avgreitt hevur}
